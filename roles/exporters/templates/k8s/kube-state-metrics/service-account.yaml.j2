---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: {{ item.name }}
    app.kubernetes.io/version: {{ item.version }}
  name: {{ item.name }}
  namespace: kube-system
